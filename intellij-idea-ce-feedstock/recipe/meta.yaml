{% set version = '181' %}

package:
  name: intellij-idea
  version: {{ version }}

source:
  git_url: git://git.jetbrains.org/idea/community.git

requirements:
  build:
    - ant
    - openjdk >=8.0.152
  run:
    - openjdk >=8.0.152
