# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/windows-default-manifest/PKGBUILD
{% set pkgname = "windows-default-manifest" %}
{% set pkgver = "6.4" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-windows-default-manifest
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/windows-default-manifest-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-windows-default-manifest
    sha256: 865a33d80d4e88f9aee66a755c51fa4bb0d4241063fcf72e6d0738b7c90b6a33

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-windows-default-manifest
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
    test:
      commands:
        - test -f $PREFIX/Library/usr/lib/default-manifest.o                     # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\lib\default-manifest.o EXIT /B 1     # [win]
    about:
      home: https://cygwin.com/
      summary: "Default Windows application manifest [repack of MSYS2-packages windows-default-manifest for MSYS]"
      license: "FSFAP"
