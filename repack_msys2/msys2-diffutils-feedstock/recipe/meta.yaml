# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/diffutils/PKGBUILD
{% set pkgname = "diffutils" %}
{% set pkgver = "3.10" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-diffutils
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/diffutils-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-diffutils
    sha256: ed5084a37995377c0288050640363a0daf1c8ace774da26ca84ec305ba51c7ce

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-diffutils
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-iconv-2.dll                            # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-bash                               >=5.2.026    # PKGBUILD sh provided by bash
        - msys2-libiconv                           >=1.17       # libiconv
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/cmp.exe                                # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\cmp.exe EXIT /B 1                # [win]
        - cmp --help                                                             # [win and x86_64]
        - diff --help                                                            # [win and x86_64]
        - diff3 --help                                                           # [win and x86_64]
        - sdiff --help                                                           # [win and x86_64]
    about:
      home: https://www.gnu.org/software/diffutils
      summary: "Utility programs used for creating patch files [repack of MSYS2-packages diffutils for MSYS]"
      license: "GPL-3.0-or-later"
