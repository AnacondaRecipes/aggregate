# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-http-cookiejar/PKGBUILD
{% set pkgname = "perl-http-cookiejar" %}
{% set pkgver = "0.014" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-http-cookiejar
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-http-cookiejar-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-http-cookiejar
    sha256: aceee3419ec000a08fc2707c2972302f7e2b6cd2160dd06c632f280f30f7838a

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-http-cookiejar
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl-http-date                     >=6.06       # perl-HTTP-Date
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/HTTP.CookieJar.3pm.gz       # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\HTTP.CookieJar.3pm.gz EXIT /B 1 # [win]
    about:
      home: http://search.cpan.org/dist/HTTP-CookieJar
      summary: "A minimalist HTTP user agent cookie jar [repack of MSYS2-packages perl-http-cookiejar for MSYS]"
      license: "Apache-2.0 OR Artistic-1.0-Perl"
