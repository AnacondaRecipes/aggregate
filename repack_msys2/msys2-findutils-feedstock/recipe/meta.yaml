# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/findutils/PKGBUILD
{% set pkgname = "findutils" %}
{% set pkgver = "4.9.0" %}
{% set pkgrel = "3" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-findutils
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/findutils-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-findutils
    sha256: 7cdbeb891be60cede94a2f9b0eb85bcf9742396b957347eb9043966d8663a831

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-findutils
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libiconv                           >=1.17       # libiconv
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/find.exe                               # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\find.exe EXIT /B 1               # [win]
        - find --help                                                            # [win and x86_64]
        - locate --help                                                          # [win and x86_64]
        - xargs --help                                                           # [win and x86_64]
    about:
      home: https://www.gnu.org/software/findutils
      summary: "GNU utilities to locate files [repack of MSYS2-packages findutils for MSYS]"
      license: "GPL-3.0-only"
