# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-http-negotiate/PKGBUILD
{% set pkgname = "perl-http-negotiate" %}
{% set pkgver = "6.01" %}
{% set pkgrel = "3" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-http-negotiate
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-HTTP-Negotiate-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-http-negotiate
    sha256: 7d436cce8fa8438b718e45abf4e9f529995c302c81893e1cbe8660bf2a15d400

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-http-negotiate
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl                               >=5.8.8      # perl>=5.8.8
        - msys2-perl-http-message                  >=6.45       # perl-HTTP-Message
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/HTTP.Negotiate.3pm.gz       # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\HTTP.Negotiate.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/dist/HTTP-Negotiate
      summary: "choose a variant to serve [repack of MSYS2-packages perl-HTTP-Negotiate for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
