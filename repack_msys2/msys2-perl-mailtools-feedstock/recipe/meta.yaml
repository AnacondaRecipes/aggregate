# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-mailtools/PKGBUILD
{% set pkgname = "perl-mailtools" %}
{% set pkgver = "2.21" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-mailtools
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-MailTools-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-mailtools
    sha256: 7076e0e48433077002a42dcec141ede4757fc20745337b3e781fb7895fe907b4

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-mailtools
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl-timedate                      >=2.33       # perl-TimeDate
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/Mail.Address.3pm.gz         # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\Mail.Address.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/release/MailTools
      summary: "Various e-mail related modules [repack of MSYS2-packages perl-MailTools for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
