# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/libxcrypt/PKGBUILD
{% set pkgname = "libxcrypt" %}
{% set pkgver = "4.4.36" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-libxcrypt
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/libxcrypt-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libxcrypt
    sha256: cbbbd3bd5ea8e1ecd5314d22cd06b284f3ae8e7d8ae3a1ffa8a863672703a2ee
  - url: https://repo.msys2.org/msys/x86_64/libxcrypt-devel-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libxcrypt-devel
    sha256: 64dadae62d822890b5b2dc4f6ec239bfce57d3959143d4f08699c0db064b1a54

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-libxcrypt
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/msys-crypt-2.dll                       # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\msys-crypt-2.dll EXIT /B 1       # [win]
    about:
      home: https://github.com/besser82/libxcrypt/
      summary: "Modern library for one-way hashing of passwords [repack of MSYS2-packages libxcrypt for MSYS]"
      license: "LGPL-2.1-or-later"
  - name: msys2-libxcrypt-devel
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libxcrypt                           =4.4.36     # libxcrypt=4.4.36
    test:
      commands:
        - test -f $PREFIX/Library/usr/include/crypt.h                            # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\include\crypt.h EXIT /B 1            # [win]
    about:
      home: https://github.com/besser82/libxcrypt/
      summary: "Modern library for one-way hashing of passwords [repack of MSYS2-packages libxcrypt-devel for MSYS]"
      license: "LGPL-2.1-or-later"
