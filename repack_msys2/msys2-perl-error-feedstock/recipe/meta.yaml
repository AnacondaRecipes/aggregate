# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-error/PKGBUILD
{% set pkgname = "perl-error" %}
{% set pkgver = "0.17029" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-error
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-Error-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-error
    sha256: c3d9d6d165a80026bee895a295979a154a221a0999669f2259b42a351f974ab7

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-error
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl                               >=5.38.2     # perl
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/Error.3pm.gz                # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\Error.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/release/Error
      summary: "Perl/CPAN Error module - Error/exception handling in an OO-ish way [repack of MSYS2-packages perl-Error for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
