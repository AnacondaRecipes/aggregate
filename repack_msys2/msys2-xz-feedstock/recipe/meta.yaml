# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/xz/PKGBUILD
{% set pkgname = "xz" %}
{% set pkgver = "5.4.6" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-xz
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/xz-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-xz
    sha256: e5f0db7c6a74e8f39f41dfa11c8b5d23b5a549b4d88d9717c0cfee9a13ef629a
  - url: https://repo.msys2.org/msys/x86_64/liblzma-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-liblzma
    sha256: 2737bf561efdf608c879e9d03a821c6ffd8d68bbf1445fefa279d6949a19debf
  - url: https://repo.msys2.org/msys/x86_64/liblzma-devel-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-liblzma-devel
    sha256: 7dc509f7bcebb536a9ce5f3f36889443efaa5bcf616912e1b9a3d588037dd402

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-xz
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
        - $RPATH/msys-lzma-5.dll                             # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-liblzma                            >=5.4.6      # liblzma
        - msys2-libiconv                           >=1.17       # libiconv
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/lzcat.exe                              # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\lzcat.exe EXIT /B 1              # [win]
        - lzcat --help                                                           # [win and x86_64]
        - lzma --help                                                            # [win and x86_64]
        - lzmadec --help                                                         # [win and x86_64]
        - lzmainfo --help                                                        # [win and x86_64]
        - unlzma --help                                                          # [win and x86_64]
        - unxz --help                                                            # [win and x86_64]
        - xz --help                                                              # [win and x86_64]
        - xzcat --help                                                           # [win and x86_64]
        - xzdec --help                                                           # [win and x86_64]
    about:
      home: https://tukaani.org/xz/
      summary: "Library and command line tools for XZ and LZMA compressed files [repack of MSYS2-packages xz for MSYS]"
      license: "CC0-1.0 AND LGPL-2.1-or-later AND GPL-2.0-or-later AND GPL-3.0-or-later"
  - name: msys2-liblzma
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/msys-lzma-5.dll                        # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\msys-lzma-5.dll EXIT /B 1        # [win]
    about:
      home: https://tukaani.org/xz/
      summary: "Library and command line tools for XZ and LZMA compressed files [repack of MSYS2-packages liblzma for MSYS]"
      license: "CC0-1.0 AND LGPL-2.1-or-later AND GPL-2.0-or-later AND GPL-3.0-or-later"
  - name: msys2-liblzma-devel
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-liblzma                            >=5.4.6      # liblzma
        - msys2-libiconv-devel                     >=1.17       # libiconv-devel
        - msys2-gettext-devel                      >=0.22.4     # gettext-devel
    test:
      commands:
        - test -f $PREFIX/Library/usr/include/lzma/base.h                        # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\include\lzma\base.h EXIT /B 1        # [win]
    about:
      home: https://tukaani.org/xz/
      summary: "Library and command line tools for XZ and LZMA compressed files [repack of MSYS2-packages liblzma-devel for MSYS]"
      license: "CC0-1.0 AND LGPL-2.1-or-later AND GPL-2.0-or-later AND GPL-3.0-or-later"
