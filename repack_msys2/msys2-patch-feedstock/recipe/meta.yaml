# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/patch/PKGBUILD
{% set pkgname = "patch" %}
{% set pkgver = "2.7.6" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-patch
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/patch-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-patch
    sha256: 6b1072509dc031c961b7984660b51a043186bb8976ee0b2aeeb08a78926962ef

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-patch
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/patch.exe                              # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\patch.exe EXIT /B 1              # [win]
        - patch --help                                                           # [win and x86_64]
    about:
      home: https://www.gnu.org/software/patch/patch.html
      summary: "A utility to apply patch files to original sources [repack of MSYS2-packages patch for MSYS]"
      license: "GPL-3.0-or-later"
