# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/c-ares/PKGBUILD
{% set pkgname = "libcares" %}
{% set pkgver = "1.27.0" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-libcares
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/libcares-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libcares
    sha256: 3904b081a047dbe1f0fe4f220fbcd06ad294d73b933183dc45057ab692f798e0
  - url: https://repo.msys2.org/msys/x86_64/libcares-devel-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libcares-devel
    sha256: 5b11a0b90c1d79c3352f5c0203c4b2201130dfde02726d8ed2b435ceeede51af

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-libcares
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-cares-2.dll                            # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-gcc-libs                           >=13.2.0     # gcc-libs
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/adig.exe                               # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\adig.exe EXIT /B 1               # [win]
    about:
      home: https://c-ares.haxx.se/
      summary: "C library that performs DNS requests and name resolves asynchronously (libraries) [repack of MSYS2-packages libcares for MSYS]"
      license: "MIT"
  - name: msys2-libcares-devel
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libcares                            =1.27.0     # libcares=1.27.0
    test:
      commands:
        - test -f $PREFIX/Library/usr/include/ares.h                             # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\include\ares.h EXIT /B 1             # [win]
    about:
      home: https://c-ares.haxx.se/
      summary: "C library that performs DNS requests and name resolves asynchronously (libraries) [repack of MSYS2-packages libcares-devel for MSYS]"
      license: "MIT"
