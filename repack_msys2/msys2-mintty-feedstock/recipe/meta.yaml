# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/mintty/PKGBUILD
{% set pkgname = "mintty" %}
{% set pkgver = "3.7.1" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-mintty
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/mintty-1~{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-mintty
    sha256: 2618c693b54da635bca7f2d73bb7563b61885713026d3f672dec2ffc4b06699b

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-mintty
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-bash                               >=5.2.026    # PKGBUILD sh provided by bash
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/mintheme                               # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\mintheme EXIT /B 1               # [win]
        - mintty --help                                                          # [win and x86_64]
    about:
      home: https://mintty.github.io
      summary: "Terminal emulator with native Windows look and feel [repack of MSYS2-packages mintty for MSYS]"
      license: "GPL-3.0-or-later"
