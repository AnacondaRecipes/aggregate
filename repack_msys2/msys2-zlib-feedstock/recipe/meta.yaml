# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/zlib/PKGBUILD
{% set pkgname = "zlib" %}
{% set pkgver = "1.3.1" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-zlib
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/zlib-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-zlib
    sha256: 8f027ac1b9404196a146efbf46ab12edf7fa20c7d9f6ca500d5cd275e492ef30
  - url: https://repo.msys2.org/msys/x86_64/zlib-devel-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-zlib-devel
    sha256: ffc86e9cc95dc5d4c16da5f5c545385af0a478b9cfe2ad1286fad8e9ddb10f3b

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-zlib
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-gcc-libs                           >=13.2.0     # gcc-libs
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/msys-z.dll                             # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\msys-z.dll EXIT /B 1             # [win]
    about:
      home: https://www.zlib.net/
      summary: "Compression library implementing the deflate compression method found in gzip and PKZIP [repack of MSYS2-packages zlib for MSYS]"
      license: "Zlib"
  - name: msys2-zlib-devel
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-zlib                                =1.3.1      # zlib=1.3.1
    test:
      commands:
        - test -f $PREFIX/Library/usr/include/zconf.h                            # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\include\zconf.h EXIT /B 1            # [win]
    about:
      home: https://www.zlib.net/
      summary: "Compression library implementing the deflate compression method found in gzip and PKZIP [repack of MSYS2-packages zlib-devel for MSYS]"
      license: "Zlib"
