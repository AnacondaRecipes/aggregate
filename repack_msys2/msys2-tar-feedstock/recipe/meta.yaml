# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/tar/PKGBUILD
{% set pkgname = "tar" %}
{% set pkgver = "1.35" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-tar
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/tar-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-tar
    sha256: 1bfdfbbf51b45f53cf6b5f6f4155fc0a8ede1d740fde7fec5764f043accd76e4

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-tar
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-iconv-2.dll                            # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libiconv                           >=1.17       # libiconv
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-bash                               >=5.2.026    # PKGBUILD sh provided by bash
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/backup                                 # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\backup EXIT /B 1                 # [win]
        - tar --help                                                             # [win and x86_64]
    about:
      home: https://www.gnu.org/software/tar/tar.html
      summary: "Utility used to store, backup, and transport files [repack of MSYS2-packages tar for MSYS]"
      license: "GPL-3.0-or-later"
