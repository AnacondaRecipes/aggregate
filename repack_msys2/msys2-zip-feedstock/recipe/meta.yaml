# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/zip/PKGBUILD
{% set pkgname = "zip" %}
{% set pkgver = "3.0" %}
{% set pkgrel = "4" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-zip
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/zip-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-zip
    sha256: 571b21408f5718e2346a3d80a9d98933b0384f9937a969194ed459a206fa1fae

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-zip
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libbz2                             >=1.0.8      # libbz2
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/zip.exe                                # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\zip.exe EXIT /B 1                # [win]
        - zip --help                                                             # [win and x86_64]
        - zipcloak --help                                                        # [win and x86_64]
    about:
      home: http://www.info-zip.org/Zip.html
      summary: "Creates PKZIP-compatible .zip files [repack of MSYS2-packages zip for MSYS]"
      license: "BSD-3-Claus"
