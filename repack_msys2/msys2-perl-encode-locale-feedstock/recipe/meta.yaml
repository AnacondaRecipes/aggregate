# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-encode-locale/PKGBUILD
{% set pkgname = "perl-encode-locale" %}
{% set pkgver = "1.05" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-encode-locale
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-Encode-Locale-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-encode-locale
    sha256: b6d0a57ca9e090073ba5a082cc0493dfbc1a85e6d9feaae70cceabadd28fd018

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-encode-locale
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl                               >=5.008      # perl>=5.008
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/Encode.Locale.3pm.gz        # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\Encode.Locale.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/dist/Encode-Locale
      summary: "Determine the locale encoding [repack of MSYS2-packages perl-Encode-Locale for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
