# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-timedate/PKGBUILD
{% set pkgname = "perl-timedate" %}
{% set pkgver = "2.33" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-timedate
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-TimeDate-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-timedate
    sha256: 4cbeab8e248a67f21ff982495678723da21adf7bb9ad51d141f09c03472baeda

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-timedate
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl                               >=5.38.2     # perl
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/Date.Format.3pm.gz          # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\Date.Format.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/dist/TimeDate
      summary: "Date formating subroutines [repack of MSYS2-packages perl-TimeDate for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
