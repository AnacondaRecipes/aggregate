# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/file/PKGBUILD
{% set pkgname = "file" %}
{% set pkgver = "5.45" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-file
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/file-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-file
    sha256: d42a01f04f668bd8de0848930c34b09fcfd5fd778c1d4c36850fc536893896b9

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-file
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-bz2-1.dll                              # [win]
        - $RPATH/msys-lzma-5.dll                             # [win]
        - $RPATH/msys-magic-1.dll                            # [win]
        - $RPATH/msys-z.dll                                  # [win]
        - $RPATH/msys-zstd-1.dll                             # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-gcc-libs                           >=13.2.0     # gcc-libs
        - msys2-zlib                               >=1.3.1      # zlib
        - msys2-libbz2                             >=1.0.8      # libbz2
        - msys2-liblzma                            >=5.4.6      # liblzma
        - msys2-libzstd                            >=1.5.5      # libzstd
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/file.exe                               # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\file.exe EXIT /B 1               # [win]
        - file --help                                                            # [win and x86_64]
    about:
      home: https://www.darwinsys.com/file/
      summary: "File type identification utility [repack of MSYS2-packages file for MSYS]"
      license: "BSD-2-Clause"
