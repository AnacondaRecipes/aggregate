# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/gawk/PKGBUILD
{% set pkgname = "gawk" %}
{% set pkgver = "5.3.0" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-gawk
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/gawk-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-gawk
    sha256: e9299fbd3b648790ed48bfe0af45d105e5972d98948d2c170b86a52ce9113ee1

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-gawk
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-gmp-10.dll                             # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
        - $RPATH/msys-mpfr-6.dll                             # [win]
        - $RPATH/msys-readline8.dll                          # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-bash                               >=5.2.026    # PKGBUILD sh provided by bash
        - msys2-mpfr                               >=4.2.1      # mpfr
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-libreadline                        >=8.2.010    # libreadline
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
        - msys2-gmp                                >=6.3.0      # msys-gmp-10.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/awk.exe                                # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\awk.exe EXIT /B 1                # [win]
        - awk --help                                                             # [win and x86_64]
        - gawk-5.3.0 --help                                                      # [win and x86_64]
        - gawk --help                                                            # [win and x86_64]
    about:
      home: https://www.gnu.org/software/gawk/
      summary: "GNU version of awk [repack of MSYS2-packages gawk for MSYS]"
      license: "GPL-3.0-only"
