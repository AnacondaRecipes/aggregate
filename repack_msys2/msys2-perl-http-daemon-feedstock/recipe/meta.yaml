# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-http-daemon/PKGBUILD
{% set pkgname = "perl-http-daemon" %}
{% set pkgver = "6.16" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-http-daemon
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-HTTP-Daemon-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-http-daemon
    sha256: 493f314ee2573916ca88d7615c00311d9ee5c901df96b356a0d319bc19f751d0

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-http-daemon
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl                               >=5.38.2     # perl
        - msys2-perl-http-date                     >=6.06       # perl-HTTP-Date
        - msys2-perl-http-message                  >=6.45       # perl-HTTP-Message
        - msys2-perl-lwp-mediatypes                >=6.04       # perl-LWP-MediaTypes
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/HTTP.Daemon.3pm.gz          # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\HTTP.Daemon.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/dist/HTTP-Daemon
      summary: "A simple http server class [repack of MSYS2-packages perl-HTTP-Daemon for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
