# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-uri/PKGBUILD
{% set pkgname = "perl-uri" %}
{% set pkgver = "5.21" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-uri
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-URI-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-uri
    sha256: b1ac37dfcb21d7c55ac2085637b5261b4187c4e27f5cd9c2beb134f90b298c61

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-uri
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl                               >=5.10.0     # perl>=5.10.0
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/URI.3pm.gz                  # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\URI.3pm.gz EXIT /B 1  # [win]
    about:
      home: https://metacpan.org/dist/URI
      summary: "Uniform Resource Identifiers (absolute and relative) [repack of MSYS2-packages perl-URI for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
