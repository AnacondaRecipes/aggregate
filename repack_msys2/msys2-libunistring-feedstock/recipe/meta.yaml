# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/libunistring/PKGBUILD
{% set pkgname = "libunistring" %}
{% set pkgver = "1.1" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-libunistring
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/libunistring-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libunistring
    sha256: e15b3b1f4a618e2212febc21e247a9a0ae1deb0bd57cc10fbb3ac9289c799136
  - url: https://repo.msys2.org/msys/x86_64/libunistring-devel-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libunistring-devel
    sha256: 280864b31b07f8e14393f04fa5373cd30cebc635440a30933d00e4fe312977ee

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-libunistring
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-iconv-2.dll                            # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libiconv                           >=1.17       # libiconv
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/msys-unistring-5.dll                   # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\msys-unistring-5.dll EXIT /B 1   # [win]
    about:
      home: https://www.gnu.org/software/libunistring/
      summary: "Library for manipulating Unicode strings and C strings. [repack of MSYS2-packages libunistring for MSYS]"
      license: "LGPL-3.0-or-later OR GPL-2.0-or-later"
  - name: msys2-libunistring-devel
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libunistring                        =1.1        # libunistring=1.1
        - msys2-libiconv-devel                     >=1.17       # libiconv-devel
    test:
      commands:
        - test -f $PREFIX/Library/usr/include/unicase.h                          # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\include\unicase.h EXIT /B 1          # [win]
    about:
      home: https://www.gnu.org/software/libunistring/
      summary: "Library for manipulating Unicode strings and C strings. [repack of MSYS2-packages libunistring-devel for MSYS]"
      license: "LGPL-3.0-or-later OR GPL-2.0-or-later"
