# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/openssh/PKGBUILD
{% set pkgname = "openssh" %}
{% set pkgver = "9.6p1" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-openssh
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/openssh-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-openssh
    sha256: 59c4e12cfa99ff9db9b23f233c54cb607cb4b9ad6952a2e028cebdb5ce21dbd6

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-openssh
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-crypt-2.dll                            # [win]
        - $RPATH/msys-crypto-3.dll                           # [win]
        - $RPATH/msys-edit-0.dll                             # [win]
        - $RPATH/msys-fido2-1.dll                            # [win]
        - $RPATH/msys-gssapi-3.dll                           # [win]
        - $RPATH/msys-kafs-0.dll                             # [win]
        - $RPATH/msys-krb5-26.dll                            # [win]
        - $RPATH/msys-z.dll                                  # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-heimdal                            >=7.8.0      # heimdal
        - msys2-libedit                            >=20221030_3.1 # libedit
        - msys2-libxcrypt                          >=4.4.36     # libxcrypt
        - msys2-libfido2                           >=1.14.0     # libfido2
        - msys2-openssl                            >=3.2.1      # openssl
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
        - msys2-libopenssl                         >=3.2.1      # msys-crypto-3.dll
        - msys2-heimdal-libs                       >=7.8.0      # msys-gssapi-3.dll
        - msys2-zlib                               >=1.3.1      # msys-z.dll
    test:
      commands:
        - test -f $PREFIX/Library/etc/ssh/moduli                                 # [unix]
        - IF NOT EXIST %PREFIX%\Library\etc\ssh\moduli EXIT /B 1                 # [win]
    about:
      home: https://www.openssh.com/portable.html
      summary: "Free version of the SSH connectivity tools [repack of MSYS2-packages openssh for MSYS]"
      license: "SSH-OpenSSH"
