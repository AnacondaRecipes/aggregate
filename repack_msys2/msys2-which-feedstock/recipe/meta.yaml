# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/which/PKGBUILD
{% set pkgname = "which" %}
{% set pkgver = "2.21" %}
{% set pkgrel = "4" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-which
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/which-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-which
    sha256: f5a64b01b801032dcfaecb80e7419ee6ff77e708fc2f6ab91bff67cdefdb4608

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-which
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-bash                               >=5.2.026    # PKGBUILD sh provided by bash
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/which.exe                              # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\which.exe EXIT /B 1              # [win]
        - which --help                                                           # [win and x86_64]
    about:
      home: https://savannah.gnu.org/projects/which/
      summary: "A utility to show the full path of commands [repack of MSYS2-packages which for MSYS]"
      license: "GPL-2.0-or-later"
