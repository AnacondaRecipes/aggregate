# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-clone/PKGBUILD
{% set pkgname = "perl-clone" %}
{% set pkgver = "0.46" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-clone
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-Clone-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-perl-clone
    sha256: 58e65544c9303f02d4bc646babc4e160c3fd9f0ec500f1e9cf7046209c7f1216

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-clone
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-gcc_s-seh-1.dll                        # [win]
        - $RPATH/msys-perl5_38.dll                           # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl                               >=5.38.2     # perl
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
        - msys2-gcc-libs                           >=13.2.0     # msys-gcc_s-seh-1.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/lib/perl5/vendor_perl/auto/Clone/autosplit.ix # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\lib\perl5\vendor_perl\auto\Clone\autosplit.ix EXIT /B 1 # [win]
    about:
      home: https://github.com/garu/Clone
      summary: "Recursive copy of nested objects. [repack of MSYS2-packages perl-Clone for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
