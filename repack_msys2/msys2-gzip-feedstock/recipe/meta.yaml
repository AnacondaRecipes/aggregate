# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/gzip/PKGBUILD
{% set pkgname = "gzip" %}
{% set pkgver = "1.13" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-gzip
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/gzip-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-gzip
    sha256: 5f2fced435165f16a65ae82c6877a57d693f69e48916b842f6a4ef51c3e3f4a2

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-gzip
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-bash                               >=5.2.026    # bash
        - msys2-less                               >=643        # less
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/gunzip                                 # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\gunzip EXIT /B 1                 # [win]
        - gzip --help                                                            # [win and x86_64]
    about:
      home: https://www.gnu.org/software/gzip/
      summary: "GNU compression utility [repack of MSYS2-packages gzip for MSYS]"
      license: "GPL-3.0-or-later"
