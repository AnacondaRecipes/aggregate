# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/msys2-launcher/PKGBUILD
{% set pkgname = "msys2-launcher" %}
{% set pkgver = "1.5" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-msys2-launcher
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/msys2-launcher-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-msys2-launcher
    sha256: 39f9dc679ffb3e514ed1ba9c38cc32c20c8f2d45fef3c0c6b73f6010e656f775

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-msys2-launcher
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-mintty                             >=2.2.1      # mintty>=1~2.2.1
    test:
      commands:
        - test -f $PREFIX/Library/clang32.exe                                    # [unix]
        - IF NOT EXIST %PREFIX%\Library\clang32.exe EXIT /B 1                    # [win]
    about:
      home: 
      summary: "Helper for launching MSYS2 shells [repack of MSYS2-packages msys2-launcher for MSYS]"
      license: "MIT"
