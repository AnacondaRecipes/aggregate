# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/make/PKGBUILD
{% set pkgname = "make" %}
{% set pkgver = "4.4.1" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-make
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/make-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-make
    sha256: d36d2f8e976fd43cb6868d79745937a7391937f70b469544b3d16cd9708765df

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-make
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-bash                               >=5.2.026    # PKGBUILD sh provided by bash
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/make.exe                               # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\make.exe EXIT /B 1               # [win]
        - make --help                                                            # [win and x86_64]
    about:
      home: https://www.gnu.org/software/make
      summary: "GNU make utility to maintain groups of programs [repack of MSYS2-packages make for MSYS]"
      license: "GPL-3.0-or-later"
