# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-mime-tools/PKGBUILD
{% set pkgname = "perl-mime-tools" %}
{% set pkgver = "5.510" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-mime-tools
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-MIME-tools-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-mime-tools
    sha256: a464ea14dfee02d26bb703bd64e1629115492588ae6ed1b7bc94557dcd9524c3

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-mime-tools
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl-mailtools                     >=2.21       # perl-MailTools
        - msys2-perl-io-stringy                                 # PKGBUILD perl-IO-stringy provided by perl-IO-Stringy
        - msys2-perl-convert-binhex                >=1.125      # perl-Convert-BinHex
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/MIME.Body.3pm.gz            # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\MIME.Body.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/release/MIME-tools
      summary: "Parses streams to create MIME entities [repack of MSYS2-packages perl-MIME-tools for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
