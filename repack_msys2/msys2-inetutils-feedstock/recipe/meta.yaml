# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/inetutils/PKGBUILD
{% set pkgname = "inetutils" %}
{% set pkgver = "2.5" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-inetutils
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/inetutils-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-inetutils
    sha256: c3cd0a60d9349692a26f738bb31e774215ddac45cca7915830095b51ac9c2be1

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-inetutils
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-ncursesw6.dll                          # [win]
        - $RPATH/msys-readline8.dll                          # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-gcc-libs                           >=13.2.0     # gcc-libs
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-libreadline                        >=8.2.010    # libreadline
        - msys2-ncurses                            >=6.4        # ncurses
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/dnsdomainname.exe                      # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\dnsdomainname.exe EXIT /B 1      # [win]
        - dnsdomainname --help                                                   # [win and x86_64]
        - ftp --help                                                             # [win and x86_64]
        - rcp --help                                                             # [win and x86_64]
        - rlogin --help                                                          # [win and x86_64]
        - rsh --help                                                             # [win and x86_64]
        - talk --help                                                            # [win and x86_64]
        - telnet --help                                                          # [win and x86_64]
    about:
      home: https://www.gnu.org/software/inetutils/
      summary: "A collection of common network programs. [repack of MSYS2-packages inetutils for MSYS]"
      license: "GPL-3.0-or-later"
