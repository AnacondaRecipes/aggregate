# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/libffi/PKGBUILD
{% set pkgname = "libffi" %}
{% set pkgver = "3.4.6" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-libffi
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/libffi-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libffi
    sha256: 88932fe3e7c7547830df2e345d3848e0aa039a1e84f9c96716a547224ebfe196
  - url: https://repo.msys2.org/msys/x86_64/libffi-devel-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libffi-devel
    sha256: 3629565e469db23d1431fa64af2c903c91e8fa686e282fba6059391c8e94ecac

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-libffi
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/msys-ffi-8.dll                         # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\msys-ffi-8.dll EXIT /B 1         # [win]
    about:
      home: https://sourceware.org/libffi/
      summary: "Portable, high level programming interface to various calling conventions [repack of MSYS2-packages libffi for MSYS]"
      license: "MIT"
  - name: msys2-libffi-devel
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libffi                              =3.4.6      # libffi=3.4.6
    test:
      commands:
        - test -f $PREFIX/Library/usr/include/ffi.h                              # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\include\ffi.h EXIT /B 1              # [win]
    about:
      home: https://sourceware.org/libffi/
      summary: "Portable, high level programming interface to various calling conventions [repack of MSYS2-packages libffi-devel for MSYS]"
      license: "MIT"
