# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-http-date/PKGBUILD
{% set pkgname = "perl-http-date" %}
{% set pkgver = "6.06" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-http-date
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-HTTP-Date-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-http-date
    sha256: 8a89f624030eaeca3d642fc3f88e36203faa6c9af6eab5e5f6ac6ad89096d7d1

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-http-date
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl                               >=5.8.8      # perl>=5.8.8
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/HTTP.Date.3pm.gz            # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\HTTP.Date.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/release/HTTP-Date
      summary: "Date conversion routines [repack of MSYS2-packages perl-HTTP-Date for MSYS]"
      license: "GPL-1.0-or-later OR Artistic-1.0-Perl"
