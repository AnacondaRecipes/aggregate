# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/nano/PKGBUILD
{% set pkgname = "nano" %}
{% set pkgver = "7.2" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-nano
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/nano-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-nano
    sha256: 43b4fb8d23c2a5ea3dcdce2c0da61694979a9f9ba21acc4511ae11241617996b

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-nano
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
        - $RPATH/msys-magic-1.dll                            # [win]
        - $RPATH/msys-ncursesw6.dll                          # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-file                               >=5.45       # file
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-ncurses                            >=6.4        # ncurses
        - msys2-bash                               >=5.2.026    # PKGBUILD sh provided by bash
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/etc/nanorc                                     # [unix]
        - IF NOT EXIST %PREFIX%\Library\etc\nanorc EXIT /B 1                     # [win]
        - nano --help                                                            # [win and x86_64]
        - rnano --help                                                           # [win and x86_64]
    about:
      home: https://www.nano-editor.org
      summary: "Pico editor clone with enhancements [repack of MSYS2-packages nano for MSYS]"
      license: "spdx:GPL-3.0-or-later"
