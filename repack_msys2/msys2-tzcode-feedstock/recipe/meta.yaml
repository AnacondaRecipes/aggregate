# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/tzcode/PKGBUILD
{% set pkgname = "tzcode" %}
{% set pkgver = "2024a" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-tzcode
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/tzcode-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-tzcode
    sha256: e93a3b501897d1e3de9efca719a9da7dc15edf7fad9ad4c9ad8753940568cc2d

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-tzcode
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-coreutils                          >=8.32       # coreutils
        - msys2-gawk                               >=5.3.0      # gawk
        - msys2-sed                                >=4.9        # sed
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/etc/localtime                                  # [unix]
        - IF NOT EXIST %PREFIX%\Library\etc\localtime EXIT /B 1                  # [win]
        - zdump --help                                                           # [win and x86_64]
        - zic --help                                                             # [win and x86_64]
    about:
      home: https://www.iana.org/time-zones
      summary: "Sources for time zone and daylight saving time data [repack of MSYS2-packages tzcode for MSYS]"
      license: "BSD 3-clause"
