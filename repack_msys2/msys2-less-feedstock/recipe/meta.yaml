# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Mar 02 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/less/PKGBUILD
{% set pkgname = "less" %}
{% set pkgver = "643" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-less
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/less-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-less
    sha256: 98ae31062a4e3cb18e697b0f3882a5b9278db2bdefc03665dd5b0d17e941ff0f

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-less
    skip: True          # [not (win and x86_64)]
    script: repack.sh   # [unix]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-ncursesw6.dll                          # [win]
        - $RPATH/msys-pcre2-8-0.dll                          # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-ncurses                            >=6.4        # ncurses
        - msys2-libpcre2_8                         >=10.43      # libpcre2_8
        - msys2-msys2-runtime                      >=3.4.10     # msys-2.0.dll
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/less.exe                               # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\less.exe EXIT /B 1               # [win]
        - less --help                                                            # [win and x86_64]
        - lessecho --help                                                        # [win and x86_64]
    about:
      home: http://www.greenwoodsoftware.com/less
      summary: "A terminal based program for viewing text files [repack of MSYS2-packages less for MSYS]"
      license: "BSD-2-Clause AND GPL-3.0-only"
