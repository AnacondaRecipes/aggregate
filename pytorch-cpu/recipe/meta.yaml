{% set version = "1.3.1" %}

package:
  name: pytorch-cpu
  version: {{ version }}

build:
  number: 0

requirements:
  run:
    - pytorch {{ version }}
    - _pytorch_select 0.1

about:
  summary: Metapackage for the CPU PyTorch variant
