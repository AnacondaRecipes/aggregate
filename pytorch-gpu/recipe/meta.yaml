{% set version = "1.3.1" %}

package:
  name: pytorch-gpu
  version: {{ version }}

build:
  number: 0

requirements:
  run:
    - pytorch {{ version }}
    - _pytorch_select 0.2

about:
  summary: Metapackage for the GPU PyTorch variant
